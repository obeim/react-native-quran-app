import { Text } from "react-native";

export function NoRecentView() {
  return (
    <Text className="text-center font-HelveticaRoman text-primary/20 dark:text-primaryDark/70">
      لم يتم قراءة شئ موخرا
    </Text>
  );
}
